import { Type } from "lucide-react";
import type { Block } from "@/core";
import type { Components } from "../types";

export const Typography: Block<Components> = {
  label: "Typography",
  icon: <Type size={16} />,
  category: "elements",
  component: "Typography",
};
